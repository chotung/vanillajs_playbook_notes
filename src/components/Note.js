class Note {
	constructor(color, btnColor) {
		this.id = id;
		this.text = "";
		this.color = color;
		this.btnColor = btnColor;
	}

	addText(input) {
		this.text = input;
	}
}

export default Note;
// id: nextId(),
// selected: false,
// color: color,
// btnColor: " #FCFE7D",
